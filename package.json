{
  "name": "shodan-geo",
  "version": "1.0.0",
  "description": "Shodan geolocation search web app",
  "main": "server.js",
  "scripts": {
    "dashboard:install": "npm --prefix client install",
    "dashboard:build": "npm --prefix client run build",
    "start": "node server.js",
    "test": "jest --coverage",
    "test:watch": "jest --watch"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "jest": "^30.3.0",
    "supertest": "^7.2.2"
  }
}
